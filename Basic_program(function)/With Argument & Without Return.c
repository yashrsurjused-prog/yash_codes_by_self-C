#include <stdio.h>

// function definition (with argument, no return)
void show(int a) {
    // printing value received
    printf("Number = %d", a);
}

int main() {
    // function call with argument
    show(10);
    return 0;
}

//output:
Number = 10
